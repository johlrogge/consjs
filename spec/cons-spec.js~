(function(){
  var defmod = function(cons, fn){
    
  };

  if(typeof defined !== 'undefined'){
      return define(['cons', 'cons/fn'], defmod);
  }
  else if (typeof require !== 'undefined') {
      return defmod(require('cons', 'cons/fn'))
      
  }
})();
